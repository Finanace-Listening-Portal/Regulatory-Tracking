| Citizen | Organization //var api_url = 'http://api.pledge.gov.in/'+'pledge/public/' +'api/v2/'; //api_url = 'https://pledgeapi.cvc.nic.in/' +'api/v2/'; var api_url = 'https://pledgeapi.mygov.in/' + 'api/v2/cvc/'; // fetch current theme and update header dynamically (function(){ $.get(api_url + 'cvc-theme/en', function(theme){ if(theme && theme['theme_name'] && theme['status'] === 'active'){ cvc_data['theme_name'] = theme['theme_name']; cvc_data['theme_id'] = theme['theme_id']; cvc_data['theme_year'] = theme['year']; $('#theme-name-en').text(theme['theme_name']); } }).fail(function(){ if(debug_mode){ console.log('Unable to fetch theme (en), using default'); } }); $.get(api_url + 'cvc-theme/hi', function(theme){ if(theme && theme['theme_name'] && theme['status'] === 'active'){ cvc_data['theme_name_hi'] = theme['theme_name']; $('#theme-name-hi').text(theme['theme_name']); } }).fail(function(){ if(debug_mode){ console.log('Unable to fetch theme (hi), using default'); } }); })(); (function(){ // hold data here var base_path = '../cvc/templates/'; var step_mapper = [ {'url':'landing.html'}, {'url': 'citizen.html'}, {'url': 'citizen-language.html'}, {'url': 'citizen-pledge.html'}, {'url': 'citizen-pledge-certificate.html'}, {'url': 'email-pledge-certificate.html'}, {'url': 'cvo-bulk-upload.html'}, {'url': 'org.html'}, {'url': 'email-bulk-pledge-certificate.html'}, {'url': 'analytics.html'}, {'url': 'citizen-pledge-source.html'} ]; var current_step = 0; function renderSection(index){ var step = step_mapper[index]; var url = base_path+step['url']; $.get( url, function( r_template ) { var template =Handlebars.compile(r_template); var html = template(cvc_data); $('div.content').remove(); $(html).insertAfter($('header')); }); } // handler for section rendering $(window).on('renderSection', function(e, data){ renderSection(data['step']); }); // route controller here $(function(){ var params = cvc_services['getAllUrlParams'].call(); // if(params['type'] && params['type'] == 'mass-pledge'){ // $(window).trigger('renderSection', {'step':6}); // }else if(params['type'] && params['type'] == 'mass-pledge-certificate'){ $(window).trigger('renderSection', {'step':8}); }else{ $(window).trigger('renderSection', {'step':0}); } }); })(); // showing stats here $(function(){ cvc_services['getPledgeStats'].call(this, 'individual', function(err, results){ var numAnim = new CountUp('individual-pledges-count', 0, results['individual'], 0); numAnim.start(); $(window).scroll(function() { if($(window).scrollTop() + $(window).height() > $(document).height() - 100) { numAnim.reset(); numAnim.update(results['individual']); } }); // $('[name=individual-pledges-count]').html(results['individual']); }); cvc_services['getPledgeStats'].call(this, 'organization', function(err, results){ var numAnim = new CountUp('org-pledges-count', 0, results['organization'], 0); numAnim.start(); $(window).scroll(function() { if($(window).scrollTop() + $(window).height() > $(document).height() - 100) { numAnim.update(results['organization']); } }); }); }) //adding click handler $('.count-type').on('click', function(){ $(window).trigger('renderSection', {'step':9}); }) window.dataLayer = window.dataLayer || []; function gtag(){dataLayer.push(arguments);} gtag('js', new Date()); gtag('config', 'G-Y9LGNJYHNQ');